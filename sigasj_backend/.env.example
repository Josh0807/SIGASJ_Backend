# Servidor
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=sigasj

# Galería — almacenamiento local de imágenes (uploads/galeria)
# Las URLs públicas se sirven bajo /uploads/galeria/{archivo}
